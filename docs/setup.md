# veeksha-docs

## Setup
```bash
pip install -r requirements.txt
```

## Run

### Build once
```bash
make html
```

An `index.html` file will be generated under `_build/html` directory. Open it in browser.

### Development with live reload
```bash
make livehtml
```

This starts a development server with automatic rebuilding and live reload when files change.
